Shared Natural tex_plaid

From "get locations" import u_t_img, u_s_scale

activetexture texture0
uniform1i u_t_img, 0

uniform1f u_s_scale, 2


tex_plaid = gentexture()

bindtexture texture_2d, tex_plaid
teximage2d texture_2d, 0, rgba, imagestore, "french"
generatemipmap texture_2d

